import { CardDividerComponent } from './card-divider.component';

describe('CardDividerComponent', () => {
  let component: CardDividerComponent;

  beforeEach(() => {
    component = new CardDividerComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
