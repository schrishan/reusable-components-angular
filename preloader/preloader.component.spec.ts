import { PreloaderComponent } from './preloader.component';

describe('PreloaderComponent', () => {
  let component: PreloaderComponent;

  beforeEach(() => {
    component = new PreloaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
