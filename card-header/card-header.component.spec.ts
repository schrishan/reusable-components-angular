import { CardHeaderComponent } from './card-header.component';

describe('CardHeaderComponent', () => {
  let component: CardHeaderComponent;

  beforeEach(() => {
    component = new CardHeaderComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
