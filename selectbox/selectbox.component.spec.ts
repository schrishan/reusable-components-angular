import { SelectboxComponent } from './selectbox.component';

describe('SelectboxComponent', () => {
  let component: SelectboxComponent;

  beforeEach(() => {
    component = new SelectboxComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
